# Server env
PORT=5000
JWT_SECRET=dev_secret_change_me
EMAIL_FROM="Spectatore <no-reply@spectatore.local>"
EMAIL_OUTBOX=./outbox_emails
CONFIRM_CODE_TTL_MIN=30

# In production, set a specific origin like https://app.yourdomain.com
# In development, the server reflects request origin automatically.
CORS_ORIGIN=

# Development only: skip email confirmation and return token on register
DEV_SKIP_EMAIL_CONFIRM=1
